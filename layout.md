/ui-refine full site width and space utilization — The entire site has excessive empty space on left and right sides. Most sections use max-w-[1200px] which wastes space on modern screens. Fix across all sections in src/components/sections/: widen the main content container from max-w-[1200px] to max-w-[1400px] consistently across Hero, Features, FeatureCards, Testimonials, Team, Blog, Contact, and CTA sections. For text-heavy sections (CTA, section descriptions) keep the text column constrained to max-w-2xl for readability but allow surrounding layout elements (grids, cards, images) to use the full wider container. For grid-based sections (Features, FeatureCards, Team, Blog, Testimonials) ensure the cards stretch to fill the wider container properly. Navigation already uses max-w-[1400px] so keep it as-is. Do not change colors, animations, or component structure — only width constraints and horizontal spacing.
